apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ .Values.tenant }}-quota
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: "2Gi"
    limits.memory: "4Gi"
    pods: "10"
